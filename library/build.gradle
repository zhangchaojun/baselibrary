plugins {
    id 'com.android.library'
    id 'maven'
//    id 'maven-publish'
}

android {
    compileSdkVersion 28

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 28
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.3.1'
    implementation 'com.google.android.material:material:1.4.0'
}

//afterEvaluate {
//    println("打印一把")
//    println(it)
//    publishing {
//        publications {
//            // Creates a Maven publication called "chaojun".
//            release(MavenPublication) {
//                println ("components是啥")
//                println (MavenPublication)
//                println (components)
////                from components.release
////                from components.java
//                groupId = 'com.github.jitpack'
//                artifactId = 'android-example'
//                version = '1.0'
//            }
//        }
//    }
//}

task comps {
    afterEvaluate {
        println("Components: " + getComponents().getNames())
    }
}

def localDefaultRepo = "file://" + new File(System.getProperty("user.home"), '.m2/repository').absolutePath
uploadArchives {
    repositories.mavenDeployer {
        // repository(url: uri('~/.m2/repository')) // 注释1
        // 配置仓库地址
        repository(url: uri(localDefaultRepo))
        // repository(url: uri('../repo'))

        // 配置 pom 信息
        pom.groupId = "com.mei.http"
        pom.artifactId = "myhttp"
        pom.version = "1.0.0-SNAPSHOT"
        pom.packaging = "aar"

        // pom.project {
        //    groupId "com.mei.http"
        //    artifactId "myhttp"
        //    version "1.0.1-SNAPSHOT"
        //    packaging "aar"
        // }
    }
}

